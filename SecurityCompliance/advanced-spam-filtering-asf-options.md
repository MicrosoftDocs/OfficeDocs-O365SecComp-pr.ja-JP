---
title: 高度なスパム フィルター処理オプション
ms.author: krowley
author: kccross
manager: laurawi
ms.date: 6/26/2015
ms.audience: ITPro
ms.topic: article
ms.service: O365-seccomp
ms.custom: TN2DMC
localization_priority: Normal
search.appverid:
- MET150
ms.assetid: b286f853-b484-4af0-b01f-281fffd85e7a
ms.collection:
- M365-security-compliance
description: 高度なスパムフィルターオプションを使用すると、管理者はメッセージのさまざまなコンテンツ属性を検査できます。メッセージにこれらの属性が存在することにより、メッセージのスパムスコアが増加する (つまり、スパムとして識別される可能性が高くなる) か、またはメッセージがスパムとしてマークされます。ASF オプションでは、HTML タグや URL リダイレクトなど、特定のメッセージプロパティを対象としています。これは、スパムメッセージで一般的に検出されます。
ms.openlocfilehash: d9333fd4f5c8937e440efb15e1a04db787b39b78
ms.sourcegitcommit: f57b4001ef1327f0ea622e716a4d7d78f1769b49
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 02/23/2019
ms.locfileid: "30218677"
---
# <a name="advanced-spam-filtering-options"></a>高度なスパム フィルター処理オプション

高度なスパムフィルターオプションを使用すると、管理者はメッセージのさまざまなコンテンツ属性を検査できます。メッセージにこれらの属性が存在することにより、メッセージのスパムスコアが増加する (つまり、スパムとして識別される可能性が高くなる) か、またはメッセージがスパムとしてマークされます。ASF オプションでは、HTML タグや URL リダイレクトなど、特定のメッセージプロパティを対象としています。これは、スパムメッセージで一般的に検出されます。
  
ASF オプションの有効化はスパム フィルターの積極的なアプローチで、このオプションによってフィルター処理されたメッセージは誤検知として報告できません。これらのメッセージは、定期的なエンドユーザー向けスパム通知によって識別し、スパム検疫から回収することができます。これらは、各 ASF オプションに固有の X-ヘッダー テキストを介して識別され、ASF オプションが一致したメッセージのインターネット ヘッダーに表示されることもあります。詳細については、「[スパム対策メッセージ ヘッダー](anti-spam-message-headers.md)」を参照してください。
  
コンテンツ フィルター ポリシーを編集するときは、ASF オプションをオン、オフ、テスト モードのいずれかに設定できます。詳細については、「[スパム フィルター ポリシーの構成](configure-your-spam-filter-policies.md)」を参照してください。テスト モードは、 **[NDR バックスキャター]**、 **[SPF レコード:Hard Fail]**、 **[条件付き Sender ID フィルター処理:ハード障害]**、および **[一括メール]** オプションには使用できません。 
  
> > [!TIP]
> ユーザーが使用している環境に基づいてスパムのブロックを最大化するために、ASF オプションをテストモードで有効にすることを検討するためのオプション情報。特定の ASF オプションのスパムの割合が高いお客様には、これらのオプションをまずテストしてから運用環境に実装することをお勧めします。> フィッシングに懸念がある組織は、[ **SPF レコード: hard fail** ] オプションをオンにすることをお勧めします。 
  
次の表では、それぞれの高度なスパム フィルター オプションについて説明します。
  
||||
|:-----|:-----|:-----|
|**高度なスパム フィルター オプション** <br/> |**説明** <br/> |**X-ヘッダー テキスト** <br/> |
|**[スパム スコアを上げる] セクション** <br/> |これらのオプションが有効な場合には、一致したメッセージの spam confidence level (SCL) を 5 または 6 に設定します。これは、スパムの疑いがあるとみなされます。メッセージに対して実行される処理は、コンテンツ フィルター ポリシーの **[スパム]** 設定と一致します。  <br/> ||
|リモート サイトへのイメージ リンク  <br/> |この設定が有効になっている場合は、リモートでリンクされた (たとえば、http を使用して) IMG タグを含む HTML コンテンツを伴うメッセージはスパム スコアが高くなります。  <br/> |X-CustomSpam:リモート サイトへのイメージ リンク  <br/> |
|URL 内の数値 IP アドレス  <br/> |この設定を有効にすると、数値ベースの URL (多くの場合は IP アドレスの形式) を含むメッセージのスパム スコアは高くなります。  <br/> |X-CustomSpam:URL 内の数値 IP  <br/> |
|別のポートに対する URL リダイレクト  <br/> |この設定を有効にすると、ポート 80 (通常の HTTP プロトコル ポート)、8080 (HTTP 代替ポート)、443 (HTTPS ポート) 以外のポートにユーザーをリダイレクトするハイパーリンクを含むメッセージのスパム スコアは高くなります。  <br/> |X-CustomSpam:別のポートに対する URL リダイレクト  <br/> |
|.biz Web サイトまたは .info Web サイトへの URL  <br/> |この設定を有効にすると、メッセージ本文に .biz または .info ドメインを含むメッセージのスパム スコアは高くなります。  <br/> |X-CustomSpam:.biz Web サイトまたは .info Web サイトへの URL  <br/> |
|**[スパムとしてマーク] セクション** <br/> |これらのオプションが有効な場合には、一致したメッセージの spam confidence level (SCL) を 9 に設定します。これは、確実にスパムであると考えられます。メッセージに対して実行される処理は、コンテンツ フィルター ポリシーの **[精度の高いスパム]** 設定と一致します。  <br/> ||
|空メッセージ  <br/> |この設定を有効にすると、本文と件名行が空で、添付ファイルもないメッセージは、スパムとしてマークされます。  <br/> |X-CustomSpam:空メッセージ  <br/> |
|HTML 内の JavaScript または VBScript  <br/> |この設定を有効にすると、HTML で JavaScript または Visual Basic Script Edition を使用するメッセージは、スパムとしてマークされます。この両方のスクリプティング言語は HTML メッセージ内で使用され、特定のアクションが自動的に実行される原因となります。ブラウザーは、残りの文書とともにスクリプトを解析して処理します。  <br/> |X-CustomSpam:HTML 内の JavaScript または VBScript タグ  <br/> |
|HTML 内の Frame タグまたは IFrame タグ  <br/> |この設定を有効にすると、\<Frame\> または \<IFrame\> HTML タグを含むメッセージはスパムとしてマークされます。このタグは、テキストやグラフィックの表示用にページをフォーマットするために、Web サイトや HTML メッセージで使用されます。  <br/> |X-CustomSpam:HTML 内の IFRAME または FRAME  <br/> |
|HTML 内の Object タグ  <br/> |この設定を有効にすると、\<Object\> HTML タグを含むメッセージはスパムとしてマークされます。この HTML タグにより、プラグインやアプリケーションが HTML ウィンドウ内で実行されます。  <br/> |X-CustomSpam:HTML 内の Object タグ  <br/> |
|HTML 内の Embed タグ  <br/> |この設定を有効にすると、\<Embed\> HTML タグを含むメッセージはスパムとしてマークされます。この HTML タグにより、さまざまなデータ型のさまざまな種類の文書が HTML 文書に埋め込まれます。その例として、音声、映画、または画像があります。  <br/> |X-CustomSpam:HTML 内の Embed タグ  <br/> |
|HTML 内の Form タグ  <br/> |この設定を有効にすると、\<Form\> HTML タグを含むメッセージはスパムとしてマークされます。この HTML タグは、Web サイトのフォームを作成するために使用されます。広告メールには、受信者から情報を要求するために、このタグが含まれていることがよくあります。  <br/> |X-CustomSpam:HTML 内の Form タグ  <br/> |
|HTML 内の Web バグ  <br/> |この設定を有効にすると、Web バグを含むメッセージはスパムとしてマークされます。Web バグは、Web ページまたは電子メール メッセージが読まれたかどうかを判別するように設計されたグラフィックです。Web バグは、一般的に 1 ピクセル角程度の小さいグラフィックとしてメッセージに追加されるため、多くの場合、受信者は Web バグを確認できません。正当なニュースレターでもこの手法が使用されることがありますが、多くはプライバシーの侵害とみなされます。  <br/> |X-CustomSpam:Web バグ  <br/> |
|機密用語の適用  <br/> |この設定を有効にすると、機密用語リスト内の用語を含むメッセージは、スパムとしてマークされます。機密用語リストを使用すると、潜在的に攻撃的なメッセージに関連する用語を簡単にブロックできます。これらの用語の一部では、大文字と小文字が区別されます。管理者はこの一覧を編集できません。機密用語一覧に対するフィルター処理は、メッセージの件名と本文の両方に適用されます。  <br/> |X-CustomSpam:件名または本文に機密用語を含むもの  <br/> |
|SPF レコード:Hard Fail  <br/> |この設定を有効にすると、SPF チェックに失敗した (SPF レコードで指定されていない IP アドレスから送信された) メッセージはスパムとしてマークされます。フィッシング メッセージの受信が懸念される組織では、この設定をオンにすることをお勧めします。  <br/> <br/>> [!NOTE]
> テストモードは、このオプションでは使用できません。  <br/> |X-CustomSpam:SPF レコードの失敗  <br/> |
|条件付き Sender ID フィルター処理:Hard Fail  <br/> |この設定を有効にすると、条件付き Sender ID チェックの結果が Hard Fail であるメッセージは、スパムとしてマークされます。このオプションでは SPF チェックと Sender ID チェックを組み合わせて、偽の送信者を含むメッセージ ヘッダーから保護できます。  <br/> <br/>> [!NOTE]
> テストモードは、このオプションでは使用できません。  <br/> |X-CustomSpam:SPF From レコードの失敗  <br/> |
|NDR バックスキャター  <br/> |オンプレミスのメールボックスを保護するために EOP を使用している場合に、この設定を有効にすると、すべての適正な配信不能レポート (NDR) メッセージが元の送信者に配信され、すべてのバックスキャター (適正でない NDR) メッセージはスパムとしてマークされます。この設定を有効にしない場合、すべての NDR がスパム フィルタリングを通過してしまいます。この場合、最も適正なメッセージは元の送信者に配信されますが、一部の (ただし、すべてではない) バックスキャター メッセージがスパムとしてマークされます。しかし、スパムとしてマークされないバックスキャター メッセージは、元の送信者には戻らず、詐称した送信者に行きます。  <br/> <br/> クラウドでホストされている Exchange Online のメールボックスを保護するためにサービスを使用している場合は、この設定を構成する必要はありません。  <br/><br/> > [!NOTE]
> どちらのシナリオ (オンプレミスとクラウドホスト型メールボックス) の場合でも、正当なバウンスメッセージである ndr が自動的に検出され、元の送信者に配信されるため、この設定を有効にする必要はありません。.> テストモードは、このオプションでは使用できません。<br/><br/>> [!TIP]
> ヒント: バックスキャターメッセージと EOP の詳細については、「[バックスキャター messages and EOP](backscatter-messages-and-eop.md)」を参照してください。          |X-customspam: backscatter NDR<br/> | |バルクメール  <br/> |バルクメールの高度なスパムフィルターは廃止され、一括および電子メールのしきい値設定に置き換えられました。詳細と設定方法については、「[迷惑メールとバルクメールの違い](what-s-the-difference-between-junk-email-and-bulk-email.md)」および「[スパムフィルターポリシーの構成](configure-your-spam-filter-policies.md)」をご覧ください。<br/> ||
   

